<section class="section">
    <div class="container">
        <h2 class="title is-2"><%= @variant.name %></h2>
        <ul>
        <% @variant.items.each do |item| %>
            <li><%= link_to item.name, item %></li>
        <% end %>
    </div>
</section>
