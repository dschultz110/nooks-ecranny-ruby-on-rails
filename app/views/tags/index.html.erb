<section class="section">
    <div class="container">
        <h2 class="title is-2">Item Tags</h2>
        <ul>
        <% @tags.each do |tag| %>
            <li><%= link_to tag.name.titleize, tag %></li>
        <% end %>
        </ul>
    </div>
</section>
